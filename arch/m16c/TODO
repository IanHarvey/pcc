* Register class b doesn't seem to be used when working with pointers.

* Code generated for registers of class c (char's) always uses the stack for
  some reason. Only one class c register is ever allocated.